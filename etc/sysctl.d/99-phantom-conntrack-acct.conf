# Enable per-flow byte/packet accounting in conntrack.
# Used by gp-lookout for per-device outbound rate anomaly detection.
# Cost: small fixed per-flow memory overhead + one extra atomic per-packet.
# Acceptable on a consumer router; privacy-neutral (already tracked flows).
net.netfilter.nf_conntrack_acct = 1
